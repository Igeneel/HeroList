function capitalize(string) {
    for (let i = 0; i < string.length; i++) {
        if (i === 0 || string[i] === " " || string[i] === "'" || string[i] === "-") {

        }
    }
    return string;
}

export {capitalize};
